extends layouts/main
include mixins/videoBlock

block content
  .videos
    each item in videosList
      +videoBlock({
        id : item.id,
        title : item.title,
        views : item.views,
        fileUrl : item.fileUrl
      })