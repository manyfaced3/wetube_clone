mixin videoBlock(video ={})
  .videoBlock
    a(href=routes.videoDetails(video.id))
      video.videoBlock__thumbnail(src=video.fileUrl, controls=true, width="250px")
      h4.videoBlock__title=video.title
      h6.videoBlock__views=video.views